@using TravelWebAppInMVC.Models.Classes
@model List<Blog>
<div class="box1-left">
    @foreach (var item in Model)
    {
        <div class="bot-img">
            <img src="@item.BlogImage" style="width:120px; height:65px;" alt="" />
        </div>
        <div class="bot-desc">
            <p>@item.Contents.Substring(0, 5)...</p>
        </div>
        <div class="clear"></div>
    }
</div>